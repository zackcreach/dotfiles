session: PORTAL
root: ~/dev/portal

windows:
  - name: " "
    commands:
      - nvim "+G"
  - name: " "
    commands:
      - stripe listen --forward-to localhost:4000/webhooks/stripe
    panes:
      - type: horizontal
        commands:
          - iex -S mix phx.server
